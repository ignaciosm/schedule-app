<% if @schedule.shifts.none? %>
  <%= link_to t('.add_shifts'),
            add_shifts_path(@schedule),
            method: :post,
            class: '' %>
<% else %>
  <%= link_to t('.remove_shifts'),
            remove_shifts_path(@schedule),
            method: :post,
            class: '',
            data: {confirm: 'Are you sure?'} %>
<% end %>
